/**
 * Only domain (plain POJO) classes must be located here.
 * These classes must not be exposed via API.
 * For API exposure use classes from {@link com.mylibrary.book.dto}
 *
 * @author Vadym Lotar
 * @since 1.0-SNAPSHOT
 */
package com.mylibrary.book.domain;